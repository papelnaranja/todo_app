<h1>Nueva Tarea</h1>

<%= form_with(model: @todo,  local: true) do |f| %>
    <%= f.label :description, "Título del post" %>
    <%= f.text_field :title %>
    <%= f.label :content, "Contenido" %>
    <%= f.text_area :content %>
    <%= f.submit "Enviar" %>
<% end %>